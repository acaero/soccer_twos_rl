{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File (Integrated Terminal)",
      "type": "debugpy",
      "request": "launch",
      "module": "src.runs.ddqn_eval",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}" // This ensures the script is run from the project root
    }
  ]
}
